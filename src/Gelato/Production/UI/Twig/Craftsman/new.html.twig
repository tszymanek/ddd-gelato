{% extends 'base.html.twig' %}

{% block body %}
    {#{{ dump() }}#}
    <div class="container">
        <div class="row">
            <h1>Craftsman creation</h1>
        </div>
        <div class="row">
            {{ form_start(form) }}
            {{ form_row(form.firstName) }}
            {{ form_row(form.lastName) }}
            <input type="submit" value="Create" />
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}